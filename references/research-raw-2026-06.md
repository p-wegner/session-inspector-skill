# Portable, offline-fähige Token-Zählung für aktuelle LLMs

## Kurzfazit

Für einen **portablen, wiederverwendbaren CLI-/Agent-Skill**, der **Codebasen und Dokumentation lokal analysiert**, ist die beste Architektur im Juni 2026 **zweistufig**: **lokal/offline** mit einem gemeinsamen, schnellen Tokenizer für Ranking, Redundanzsuche und Prompt-Verdichtung; **optional online** mit den offiziellen Count-APIs der Anbieter für den letzten, exakten Preflight vor dem eigentlichen Modellaufruf. Für **OpenAI** ist `tiktoken` weiterhin die belastbarste lokale Lösung. Für **Gemini** gibt es inzwischen in Googles offizieller Python-SDK-Implementierung einen lokalen Tokenizer-Pfad auf Basis von **SentencePiece** bzw. Hugging-Face-Tokenizern, allerdings mit modellabhängiger Abdeckung und möglichem Lag bei ganz neuen Modell-IDs. Für **Claude** gibt es dagegen **keinen offiziellen, aktuellen lokalen Tokenizer**; Anthropic verweist für genaue Zählung auf `client.messages.count_tokens`, und das alte offizielle Paket ist laut eigener README seit Claude 3 nur noch eine „very rough approximation“. Deshalb sollte Claude in einem Offline-First-Skill als **approximiertes Ziel** behandelt werden, nicht als lokal exakt zählbarer Provider. citeturn26view0turn18view0turn21view0turn35view0turn39view1turn40view2turn44search2

## Anthropic Claude

Anthropic bietet für aktuelle Claude-Modelle eine offizielle Zählmöglichkeit über die **Messages-API** an; die Dokumentation zeigt dafür den SDK-Aufruf `client.messages.count_tokens(...)`. Diese Zählung ist **netzwerkgebunden**, da sie eine API-Anfrage ist, und sie ist laut Anthropic **kostenlos**. Gleichzeitig sagt Anthropic ausdrücklich, dass **Token-Counting-Requests gegen die normalen Rate Limits zählen**. Für einen Build-/Skill-Kontext heißt das: **online exakt, offline nicht**. citeturn1view0turn1view1turn2view0turn2view2

Zur Modellfrage ist wichtig: In den von mir geprüften öffentlichen Anthropic-Unterlagen wird ein **„newest tokenizer“** erwähnt, der **ab Claude Opus 4.7** eingeführt wurde und auch für **Fable 5** und **Mythos 5** verwendet wird. Ich habe in diesem Material **keine offizielle, öffentliche Aussage gefunden**, die diesen aktuellen Claude-Tokenizer einer offen benannten Familie wie „BPE“ oder „SentencePiece“ zuordnet. Anders gesagt: Anthropic sagt öffentlich, **dass es einen neuen Tokenizer für die aktuellen Generationen gibt**, aber ich habe **keine offizielle Spezifikation der Tokenizer-Familie** gefunden. citeturn0search12

Zur lokalen Lage ist die Antwort für Claude derzeit klar: **Es gibt keinen offiziellen, präzisen lokalen Tokenizer für die heutigen Claude-Generationen.** Das alte offizielle Repo `anthropics/anthropic-tokenizer-typescript` sagt in seiner README selbst, dass es nur für **ältere Anthropic-Modelle** taugt und **„as of the Claude 3 models“ nicht mehr akkurat** ist; es könne nur noch als **„very rough approximation“** dienen. Das passende NPM-Paket `@anthropic-ai/tokenizer` ist ebenfalls alt; auf npm wird **Version 0.0.4** mit Veröffentlichung aus **2023** angezeigt. Für Claude 4/5 ist das also **kein guter Sockel** für einen belastbaren 2026er Skill. citeturn44search2turn44search1

Es gibt Community-Approximationsansätze. Ein aktuelles Beispiel ist `bpe-lite`, das sich als **offline**, **zero-dependency** und **mehrere Familien** unterstützend beschreibt; der begleitende Benchmark-Artikel sagt aber ausdrücklich, dass der Anthropic-Teil auf einer **reverse-engineerten Approximation** basiert. Das ist für **relative Analysen** nützlich, aber nicht für **harte Kontextgrenzen** oder **Abrechnungsnähe**. Für Claude ist daher die nüchterne 2026-Einschätzung: **offiziell exakt nur per API**, offline lokal nur **heuristisch/approximativ**. citeturn9search1turn8view4

## OpenAI

OpenAI hat 2026 zwei Schienen: eine **offizielle API-Zählung** und eine **starke lokale Bibliothek**. Die offizielle Doku beschreibt ein eigenes **Input-Token-Count-Endpoint** unter `POST /v1/responses/input_tokens`. Dieses Endpoint akzeptiert das **gleiche Input-Format wie die Responses API** und liefert laut Doku den **exakten Input-Token-Count**, den das Modell tatsächlich sehen wird — inklusive komplexerer Inputs wie **Messages, Bilder, Dateien, Tools und Conversations**. OpenAI begründet das explizit damit, dass lokale Tokenizer für **Bilder, Dateien, Tools/Schemas und model-spezifisches Verhalten** Grenzen haben. Das macht den Endpoint zum besten Online-Preflight für OpenAI. citeturn26view0

Die aktuelle Modellseite bestätigt zugleich, dass die 2026er Frontier-Linie öffentlich als **GPT-5.5**, **GPT-5.4**, **GPT-5.4 mini** und **GPT-5.4 nano** geführt wird; GPT-5.5 und GPT-5.4 haben dabei **1M Kontextfenster**, GPT-5.4 mini **400K**. Für die ältere GPT-5-Seite ist zusätzlich ein **400K Kontextfenster** dokumentiert. Das heißt: Die Modellfamilie, nach der du fragst, ist real und offiziell dokumentiert. citeturn14view0turn14view1turn30search1

Lokal ist `tiktoken` weiterhin der Referenzstandard. Das offizielle Repo beschreibt `tiktoken` als **schnellen BPE-Tokenizer** für OpenAI-Modelle; die aktuelle Open-Source-Version ist laut Repo **0.13.0** vom **15. Mai 2026**. Wichtig ist aber die feine Nuance bei den neuesten IDs: Das aktuelle `model.py` auf `main` mappt **`gpt-5`** und Präfixe wie **`gpt-5-...`** auf **`o200k_base`**, ebenso **`o1`**, **`o3`** und **`o4-mini`** auf `o200k_base`. In dem von mir geprüften Stand fehlt aber die explizite Unterstützung für **punktierte neue IDs** wie `gpt-5.4` oder `gpt-5.5`; offene PRs und Issues zeigen, dass genau dafür Mapping-Erweiterungen (`"gpt-5." -> "o200k_base"`) diskutiert wurden. Praktisch bedeutet das: **Ja, `tiktoken` deckt die GPT-5-Linie weiterhin tokenizer-seitig ab**, aber **`encoding_for_model("gpt-5.5")` kann je nach Release-Stand hinterherhinken**; als robuster Fallback solltest du dann **direkt `o200k_base`** laden. citeturn18view0turn21view0turn17search1turn17search2

Für Offline-Fähigkeit ist `tiktoken` brauchbar, aber nicht „magisch self-contained“. Das offizielle Quellmaterial zeigt, dass die OpenAI-Encodings wie `o200k_base` und `cl100k_base` beim ersten Gebrauch aus **OpenAI-Blob-URLs** geladen werden; `tiktoken` hat dafür eine **Cache-Logik** mit `TIKTOKEN_CACHE_DIR` bzw. Default-Temp-Cache. Danach läuft es lokal aus dem Cache. Wenn du einen wirklich **offline bundelbaren Skill** bauen willst, solltest du die `.tiktoken`-Artefakte **vorab cachen oder mit ausliefern**, statt auf First-Run-Download zu hoffen. citeturn23search0turn24search0

Zur API-Kostenseite ist die Lage gemischt: Die OpenAI-Doku beschreibt das Count-Endpoint ausführlich, aber ich habe in den offiziellen Pricing-Seiten **keine eigene Preiszeile** für `responses.input_tokens.count` gefunden. Ein Community-Antwortender im OpenAI-Forum sagt, dieses Endpoint sei **kostenlos**; als harte offizielle Aussage habe ich das jedoch in den offiziellen Seiten, die ich geprüft habe, **nicht wörtlich gefunden**. Für Architekturentscheidungen ist daher die belastbare Aussage: **es existiert**, **es ist der exakte Online-Weg**, und **es braucht Netzwerk/API-Key**. citeturn26view0turn28search0turn29search1

## Google Gemini

Google dokumentiert für Gemini ebenfalls eine offizielle **`countTokens`-Methode**. In der allgemeinen Methodenseite ist sie als **`POST /v1beta/{model=models/*}:countTokens`** aufgeführt; die Token-Guide-Seite erklärt, dass `count_tokens` die **Input-Tokens** zurückliefert, während `usage_metadata` nach `generate_content` die **Input-, Output- und ggf. Thinking-/Cache-Tokens** liefert. Die Doku betont außerdem, dass Gemini **multimodal** tokenisiert; Bilder und andere Nicht-Text-Inputs werden also mitgerechnet. Für einen exakten Online-Preflight ist das die richtige Quelle der Wahrheit. citeturn35view3turn35view0

Die aktuellen offiziellen Google-Unterlagen zeigen zudem, dass `countTokens` für eine breite Modellmenge unterstützt wird, darunter **Gemini 2.5 Pro**, **Gemini 2.5 Flash**, **Gemini 2.5 Flash-Lite**, **Gemini 3.1 Flash-Lite**, **Gemini 3.1 Pro (preview)**, **Gemini 3.5 Flash** und weitere Varianten. Die allgemeinen Rate-Limits von Gemini sind projekt- und modellabhängig und werden entlang von **RPM**, **Input-TPM** und **RPD** definiert. Das ist für einen Skill wichtig, wenn du Online-Preflight im Batch oder parallel einsetzen willst. citeturn35view1turn35view2

Interessant für dein Offline-Ziel ist, dass Google inzwischen in der **offiziellen Python-SDK-Implementierung** einen **lokalen Tokenizer-Lader** mitbringt. Das Repo `googleapis/python-genai` ist aktuell aktiv und zeigt auf GitHub **v2.8.0** vom **3. Juni 2026**. In `_local_tokenizer_loader.py` mappt Google mehrere Gemini-Modelle auf lokale Tokenizer-Backends: **Gemini 2.5 Pro/Flash/Flash-Lite** sowie **Gemini 2.0 Flash/Flash-Lite** werden auf einen **Gemma-3-basierten SentencePiece-Tokenizer** gemappt; neuere Einträge wie **Gemini 3.5 Flash**, **Gemini 3.1 Flash-Lite** und **Gemini 3.1 Pro Preview** werden über **Hugging-Face-Tokenizer/AutoProcessor** geladen. Das ist der stärkste offizielle lokale Weg, den ich für Gemini gefunden habe. citeturn39view1turn40view0turn40view2turn40view3

Die Kehrseite: Auch dieses lokale Gemini-Setup ist **nicht vollkommen „out of the box offline“**. Die offizielle Loader-Implementierung ruft **`requests.get(...)`** auf und lädt Tokenizer-Artefakte von externen URLs, bevor sie gecacht werden. Außerdem zeigen Issues im selben offiziellen Repo, dass die **Modellabdeckung zeitweise hinter neuen Releases herhing**. Für dein Bundle heißt das: Wenn du diesen Weg wählst, solltest du die benötigten **SentencePiece-/HF-Artefakte explizit vendoren oder im Build vorab cachen**. Dann ist ein echter Offline-Betrieb erreichbar; ohne dieses Packaging ist es eher **offline-capable after warm-up** als **self-contained offline**. citeturn40view0turn40view2turn38search2turn38search6

## Portable Open-Source-Werkzeuge

Für offene, portable Optionen ist die Landschaft 2026 asymmetrisch. **Hugging Face Transformers** dokumentiert klar einen **Offline-Modus**: Du kannst Modelle/Tokenizer vorab mit `snapshot_download` cachen, `HF_HUB_OFFLINE=1` setzen oder `local_files_only=True` verwenden. `AutoTokenizer.from_pretrained(...)` bleibt das Standardmuster, wenn du modellnahe Tokenizer lokal laden möchtest. Für offene Modelle und provider-nahe Replikate ist das sehr praktisch. Für proprietäre Hosted-Modelle wie Claude oder GPT ersetzt das aber **nicht automatisch** die offiziellen Provider-Counting-Wege. citeturn41search1turn41search5

`ttok` von Simon Willison existiert weiterhin und ist als kleines CLI nützlich, aber seine README ist inhaltlich klar **OpenAI-/tiktoken-zentriert**. Die dokumentierte Modellauswahl in der README endet effektiv bei älteren OpenAI-Linien wie `gpt-4`, `gpt-3.5-turbo`, `text-embedding-*` und früheren Encodings. Für ein 2026er, **mehrproviderfähiges** Skill ist das daher **kein guter Fundament-Baustein**, auch wenn es als kleines Hilfs-CLI für OpenAI/Tiktoken weiter brauchbar ist. citeturn43view0

Im **Node-/JS-Umfeld** sind die stärksten Bausteine für OpenAI heute `gpt-tokenizer`, `js-tiktoken` und das offizielle npm-Paket `tiktoken`. `gpt-tokenizer` beschreibt sich selbst als **TypeScript-BPE-Tokenizer** für aktuelle OpenAI-Modelle inklusive **GPT-5**, **o-series**, **GPT-4o** und **GPT-4.1**, unterstützt `o200k_base` und `o200k_harmony`, läuft synchron, browserfähig und ohne Python. Das offizielle npm-Paket `tiktoken` verweist auf **`js-tiktoken`** als **Pure-JS-Port** für Umgebungen, in denen WASM weniger gut passt; `js-tiktoken` ist auf npm weiterhin verfügbar und wurde zuletzt **vor 6 Monaten** veröffentlicht. Für einen Node-first-Stack ist das die robusteste OpenAI-Seite. citeturn43view1turn44search0turn44search3

Für Anthropic ist der Zustand im JS-Ökosystem deutlich schwächer. Das offizielle `@anthropic-ai/tokenizer` ist alt, und das Repo sagt selbst, dass es für Claude 3+ nur noch grob approximiert. Wenn du **einen einzigen JS-only Approximation-Stack** über OpenAI, Claude und Gemini willst, ist der interessanteste Kandidat, den ich gefunden habe, **`bpe-lite`**: Das Repo beschreibt es als **offline**, **pure JS**, **zero dependency** und auf **OpenAI, Anthropic und Gemini** ausgerichtet. Der dazugehörige Artikel sagt aber offen, dass **Anthropic reverse-engineert** und **Gemini auf Gemma-SPM** approximiert wird. Das ist nützlich für **relative Prompt-Hygiene**, aber nicht die gleiche Qualitätsklasse wie offizielle Count-APIs. citeturn44search2turn9search1turn8view4

## Genauigkeit, Heuristiken und der sinnvolle Kompromiss

Für grobe Planung sind die altbekannten Heuristiken auch 2026 noch erstaunlich brauchbar. OpenAI nennt in der Help-Center-Seite weiter ungefähr **1.500 Wörter ≈ 2.048 Tokens** und **1 Absatz ≈ 100 Tokens**; das `tiktoken`-Repo sagt außerdem, dass in der Praxis ein Token im Mittel ungefähr **4 Bytes** entspricht. Google schreibt für Gemini explizit: **1 Token ≈ 4 Zeichen** und **100 Tokens ≈ 60–80 englische Wörter**. Über Anbieter hinweg ist damit eine brauchbare Faustregel für englischen Fließtext weiterhin ungefähr **0,7–0,8 Wörter pro Token** bzw. **rund 4 Zeichen pro Token**. Für Code bestätigt Googles Long-Context-Seite dieselbe Größenordnung indirekt mit **50.000 Zeilen Code à 80 Zeichen ≈ 1 Mio. Tokens**. citeturn14view4turn14view5turn35view0turn36search4

Die entscheidende Abwägung ist deshalb nicht „exakt oder unbrauchbar“, sondern **wofür** du die Zahl brauchst. Wenn dein Skill primär **Redundanz, Wiederholung, Boilerplate, aufgeblähte Kommentare, lange READMEs, ausufernde Changelogs und unnötige Prompt-Segmente** finden soll, dann ist eine **gemeinsame lokale Normalisierungsmetrik** meist völlig ausreichend. Dafür ist ein einzelner, schneller Tokenizer wie **`o200k_base`** hervorragend geeignet: Er ist modern, OpenAI-seitig exakt, multilingual halbwegs effizient, und du kannst damit Code/Doku in eine konsistente Größe bringen, um Einsparpotenziale zu ranken. Für diese Aufgabe ist die **relative Ordnung** wichtiger als die **absolute provider-spezifische Endzahl**. citeturn18view0turn26view0turn14view5

Bei **Claude** ist die Lage anders: Ich habe **keinen offiziellen publizierten Vergleich** gefunden, der seriös sagt, wie weit `o200k_base` im Mittel von den aktuellen Claude-Zählungen abweicht. Was ich gefunden habe, spricht eher für Vorsicht: Anthropic selbst sagt, sein altes offizielles Local-Paket sei für Claude 3+ nur noch **„very rough“**, und die moderneren Community-Lösungen beschreiben sich als **reverse-engineered approximations**. Daraus folgt eine sinnvolle Engineering-Inferenz: Nutze einen gemeinsamen lokalen Tokenizer wie `o200k_base` ruhig für **Ranking und Kompression**, aber wenn das Endziel **Claude** ist und du **offline bleiben musst**, plane eine **deutliche Sicherheitsreserve**, etwa im Bereich **15–20 %**, statt bis an die Kontextgrenze zu optimieren. Das ist **keine offizielle Anthropic-Zahl**, sondern eine konservative Betriebsregel aus der verfügbaren Evidenz. citeturn44search2turn9search1turn8view4turn1view0

Für **Gemini** bist du besser gestellt: Hier gibt es zum einen die offizielle `countTokens`-API, zum anderen einen offiziellen lokalen Python-Pfad über Googles SDK. Trotzdem würde ich auch dort unterscheiden zwischen **lokaler Vorauswahl** und **online finaler Verifikation**, wenn ein Request wirklich an Produktionsgrenzen kratzt oder multimodale Inputs, Thinking, Caching oder Tooling eine Rolle spielen. Bei **OpenAI** ist die Trennlinie am saubersten: lokal `tiktoken` für Text, online `responses.input_tokens.count` für exakte multimodale/tool-aware Realität. citeturn35view0turn40view2turn26view0

## Konkrete Empfehlung

Die **beste konkrete Stack-Empfehlung** für deinen Skill ist eine **Python-basierte, offline-first Architektur mit optionalen Online-Exactors**:

- **Primärer lokaler Normalizer:** `tiktoken` mit **direktem Laden von `o200k_base`** statt blindem `encoding_for_model(...)`. Bundle oder pre-cache die `.tiktoken`-Vokabulare, damit der Skill wirklich offline läuft. Das gibt dir eine schnelle, portable, moderne Metrik für Code und Dokumentation. citeturn18view0turn23search0turn24search0
- **OpenAI exakt lokal:** Für reine Text-/Chat-Previews bleibt `tiktoken` dein exakter OpenAI-Zähler. Für komplexe Inputs oder letzte Verifikation nutze optional `responses.input_tokens.count`. citeturn26view0turn18view0
- **Gemini lokal möglichst exakt:** Integriere den offiziellen lokalen Tokenizer-Pfad aus `google-genai` **nur für die Modelle, die du tatsächlich targetest**, und vendor die benötigten SentencePiece-/HF-Dateien. Nutze `countTokens` nur als optionalen Online-Preflight. citeturn39view1turn40view0turn40view2turn35view3
- **Claude offline nur approximativ:** Verwende **denselben `o200k_base`-Count** als **relative Metrik** für Claude-orientierte Optimierung. Wenn online verfügbar, rechne den finalen Request mit `client.messages.count_tokens` gegen. Verlasse dich **nicht** auf `@anthropic-ai/tokenizer` für Claude 4/5. citeturn1view0turn2view0turn44search2

Für eine saubere Skill-Architektur würde ich **zwei Zahlen pro Chunk/Datei** speichern: erstens **`normalized_tokens_o200k`** als providerübergreifende Vergleichszahl; zweitens optional **`provider_tokens`** für OpenAI/Gemini/Claude, wenn ein exakter oder provider-näherer Zähler existiert. Damit kannst du lokal schnell analysieren, was Prompt-Budget frisst, und erst beim finalen Versand — falls Netzwerk vorhanden ist — die exakte Anbieterzahl einholen. Diese Trennung ist für deinen Anwendungsfall besser als der Versuch, alle Anbieter zwanghaft in einen scheinbar exakten lokalen Serializer zu pressen. citeturn26view0turn35view0turn1view0

Wenn du **unbedingt Node-first** bauen willst, ist die realistische Empfehlung: **`gpt-tokenizer`** für OpenAI und als genereller `o200k_base`-Backbone; **kein** `ttok` als Kern; **kein** `@anthropic-ai/tokenizer` für moderne Claude-Targets; und nur dann ein **einheitliches Community-Paket** wie `bpe-lite`, wenn du bewusst akzeptierst, dass Claude und Teile von Gemini **nur approximiert** werden. Für ein professionelles, wiederverwendbares Agent-Skill-Artifact würde ich trotzdem **Python** bevorzugen, weil du dort heute die beste Kombination aus `tiktoken`, Googles offiziellem lokalem Tokenizer-Pfad und einfacher CLI-Bundling-Story bekommst. citeturn43view1turn43view0turn44search2turn9search1turn39view1

## Offene Fragen und Grenzen

Ein paar Punkte bleiben trotz der recherchierten Quellen bewusst offen. Ich habe **keine offizielle Anthropic-Spezifikation** gefunden, die den aktuellen Claude-/Fable-Tokenizer öffentlich als bestimmte Familie wie BPE oder SentencePiece benennt. Ich habe außerdem **keinen offiziellen, publizierten Benchmark** gefunden, der seriös quantifiziert, wie weit ein gemeinsamer Tokenizer wie `o200k_base` bei aktuellen Claude-Modellen vom realen Count abweicht. Und bei Google ist zwar ein offizieller lokaler Tokenizer-Pfad vorhanden, aber seine **Modellabdeckung ist code-/releaseabhängig** und kann bei sehr neuen Modellnamen hinterherhinken. Genau deshalb ist die oben empfohlene Architektur absichtlich **offline-first, aber online-verifizierbar** gebaut. citeturn0search12turn44search2turn38search2turn38search6